import { BookOpen } from "lucide-react";
import { NotBuiltYet } from "@/components/page/not-built-yet";

export default function Page() {
  return <NotBuiltYet description="Khóa học bạn phụ trách hoặc đã tạo." icon={BookOpen} title="Khóa học" />;
}
